---
phase: 08-facemodelmorph-node
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - __init__.py
  - tests/test_face_model_morph.py
autonomous: true
requirements:
  - MRPH-02
  - MRPH-01

must_haves:
  truths:
    - "FaceModelMorph appears in NODE_CLASS_MAPPINGS and NODE_DISPLAY_NAME_MAPPINGS"
    - "FaceModelMorph is importable from comfyui_imgtools package"
    - "head_scale in output align_data matches expected value from model vs source head dimensions"
    - "Output interface (IMAGE, MASK, ALIGN_DATA) makes FaceModelMorph a drop-in replacement for FaceShapeMorph"
    - "Full test suite (all phases) passes with zero regressions"
  artifacts:
    - path: "__init__.py"
      provides: "FaceModelMorph node registration"
      contains: "FaceModelMorph"
    - path: "tests/test_face_model_morph.py"
      provides: "Registration and integration tests"
      contains: "TestRegistration"
  key_links:
    - from: "__init__.py"
      to: "face_model_morph.py"
      via: "from .face_model_morph import FaceModelMorph"
      pattern: "FaceModelMorph"
    - from: "face_model_morph.py"
      to: "face_composite.py"
      via: "align_data['head_scale'] consumed by FaceComposite"
      pattern: "head_scale"
---

<objective>
Register FaceModelMorph node in ComfyUI and add integration/registration tests to confirm drop-in replacement and head_scale passthrough.

Purpose: Wire the FaceModelMorph class (built in plan 01) into the ComfyUI node system and validate it works as a drop-in replacement for FaceShapeMorph with correct head_scale propagation for FaceComposite (MRPH-02).

Output: Updated `__init__.py` with FaceModelMorph registration. Extended test file with registration tests and head_scale validation. Full suite green.
</objective>

<execution_context>
@/Users/rui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-facemodelmorph-node/08-01-SUMMARY.md

@__init__.py
@face_morph.py
@face_model_morph.py

<interfaces>
<!-- From __init__.py — current registration pattern: -->
```python
from .face_model_builder import FaceModelBuilder
# ... inside if _face_nodes_available:
NODE_CLASS_MAPPINGS["FaceModelBuilder"] = FaceModelBuilder
NODE_DISPLAY_NAME_MAPPINGS["FaceModelBuilder"] = "ImgTools Face Model Builder"
```

<!-- From face_morph.py — output contract to match: -->
```python
RETURN_TYPES = ("IMAGE", "MASK", "ALIGN_DATA")
RETURN_NAMES = ("morphed_face", "warp_mask", "align_data")
```

<!-- head_scale consumption pattern from face_composite.py: -->
```python
out_align_data["head_scale"] = float(head_scale)
# FaceComposite reads: align_data.get("head_scale", 1.0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register FaceModelMorph in __init__.py and add registration tests</name>
  <files>__init__.py, tests/test_face_model_morph.py</files>
  <action>
    1. In `__init__.py`, add `from .face_model_morph import FaceModelMorph` in the try block (same line group as FaceModelBuilder import).
    2. Inside the `if _face_nodes_available:` block, add:
       - `NODE_CLASS_MAPPINGS["FaceModelMorph"] = FaceModelMorph`
       - `NODE_DISPLAY_NAME_MAPPINGS["FaceModelMorph"] = "ImgTools Face Model Morph"`
    3. In `tests/test_face_model_morph.py`, add a `TestRegistration` class with tests:
       - `test_node_registered`: import `__init__` module, assert "FaceModelMorph" in NODE_CLASS_MAPPINGS
       - `test_display_name`: assert NODE_DISPLAY_NAME_MAPPINGS["FaceModelMorph"] == "ImgTools Face Model Morph"
       - `test_drop_in_replacement`: verify FaceModelMorph.RETURN_TYPES == FaceShapeMorph.RETURN_TYPES and FaceModelMorph.RETURN_NAMES == FaceShapeMorph.RETURN_NAMES
    4. Add a `TestHeadScalePassthrough` class:
       - `test_head_scale_in_align_data`: run morph() with synthetic inputs, verify output align_data contains "head_scale" key with a float value
       - `test_head_scale_interpolated_by_strength`: run at strength=0.0, verify head_scale == 1.0 (no change). Run at strength=1.0, verify head_scale != 1.0 (when model and source have different head dimensions)
  </action>
  <verify>
    <automated>conda run -n comfyui pytest tests/test_face_model_morph.py -x -v</automated>
  </verify>
  <done>FaceModelMorph registered in ComfyUI node system. Registration and head_scale tests pass. Drop-in replacement confirmed by matching RETURN_TYPES/RETURN_NAMES with FaceShapeMorph.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression test suite</name>
  <files></files>
  <action>
    Run the full test suite to confirm zero regressions from Phase 8 changes. All existing v1.0 and v1.1 tests must pass alongside the new FaceModelMorph tests. If any test fails, diagnose and fix without breaking other tests.
  </action>
  <verify>
    <automated>conda run -n comfyui pytest tests/ -x -v</automated>
  </verify>
  <done>Full test suite passes. Zero regressions from Phase 8. FaceModelMorph tests integrated alongside all existing tests.</done>
</task>

</tasks>

<verification>
conda run -n comfyui pytest tests/ -x -v
</verification>

<success_criteria>
- FaceModelMorph in NODE_CLASS_MAPPINGS with display name "ImgTools Face Model Morph"
- RETURN_TYPES and RETURN_NAMES match FaceShapeMorph exactly (drop-in replacement)
- head_scale present in output align_data, interpolated by effective_strength
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-facemodelmorph-node/08-02-SUMMARY.md`
</output>
