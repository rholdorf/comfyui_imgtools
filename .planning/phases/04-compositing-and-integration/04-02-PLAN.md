---
phase: 04-compositing-and-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - __init__.py
autonomous: false
requirements: [COMP-01, COMP-04]

must_haves:
  truths:
    - "FaceComposite is registered in NODE_CLASS_MAPPINGS as 'ImgTools Face Composite'"
    - "FaceComposite is gated behind _face_nodes_available check"
    - "Full 3-node pipeline (FaceCropAlign -> FaceShapeMorph -> FaceComposite) can execute without errors"
  artifacts:
    - path: "__init__.py"
      provides: "FaceComposite node registration"
      contains: "FaceComposite"
  key_links:
    - from: "__init__.py"
      to: "face_composite.py"
      via: "import FaceComposite"
      pattern: "from .face_composite import FaceComposite"
---

<objective>
Register FaceComposite in ComfyUI node mappings and verify the full pipeline works end-to-end.

Purpose: Makes the node available in ComfyUI and validates the complete face shape matching workflow.
Output: Updated __init__.py with FaceComposite registration. Full pipeline validated.
</objective>

<execution_context>
@/Users/rui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compositing-and-integration/04-01-SUMMARY.md

@__init__.py
@face_composite.py

<interfaces>
<!-- Registration pattern from __init__.py -->
```python
# Existing pattern:
from .face_morph import FaceShapeMorph
# ...
if _face_nodes_available:
    NODE_CLASS_MAPPINGS["FaceShapeMorph"] = FaceShapeMorph
    NODE_DISPLAY_NAME_MAPPINGS["FaceShapeMorph"] = "ImgTools Face Shape Morph"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register FaceComposite in __init__.py</name>
  <files>__init__.py</files>
  <action>
    Add FaceComposite to the existing registration pattern in __init__.py:

    1. Add `from .face_composite import FaceComposite` inside the existing try block (after the FaceShapeMorph import)
    2. Add to NODE_CLASS_MAPPINGS inside the `if _face_nodes_available:` block:
       `NODE_CLASS_MAPPINGS["FaceComposite"] = FaceComposite`
    3. Add to NODE_DISPLAY_NAME_MAPPINGS:
       `NODE_DISPLAY_NAME_MAPPINGS["FaceComposite"] = "ImgTools Face Composite"`

    Follow the exact same pattern as FaceDetect, FaceCropAlign, and FaceShapeMorph registrations.
  </action>
  <verify>
    <automated>cd /Users/rui/src/pg/ComfyUI/custom_nodes/comfyui_imgtools && python -c "from comfyui_imgtools import NODE_CLASS_MAPPINGS; assert 'FaceComposite' in NODE_CLASS_MAPPINGS; print('FaceComposite registered successfully')" && pytest tests/ -x -v</automated>
  </verify>
  <done>FaceComposite appears in NODE_CLASS_MAPPINGS and NODE_DISPLAY_NAME_MAPPINGS. All existing tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete face shape matching pipeline: FaceCropAlign -> FaceShapeMorph -> FaceComposite. All 4 face nodes (FaceDetect, FaceCropAlign, FaceShapeMorph, FaceComposite) registered and available in ComfyUI.</what-built>
  <how-to-verify>
    1. Start ComfyUI: `cd /Users/rui/src/pg/ComfyUI && python main.py`
    2. In the ComfyUI web UI, verify all 4 nodes appear under "imgtools/face" category:
       - ImgTools Face Detect
       - ImgTools Face Crop Align
       - ImgTools Face Shape Morph
       - ImgTools Face Composite
    3. Build the full pipeline:
       - Load a source face image and a target face image
       - FaceDetect(source) -> FaceCropAlign(source) -> source crop
       - FaceDetect(target) -> FaceCropAlign(target) -> target crop
       - FaceShapeMorph(source_crop, target_crop, ...) -> morphed face
       - FaceComposite(original=source, morphed_face, warp_mask, align_data) -> composited result
    4. Verify the composited image shows the source face with shape morphed toward target, blended naturally into the original image
    5. Try strength=0: result should look nearly identical to original
    6. Try strength=1: result should show maximum shape change
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the composited result</resume-signal>
</task>

</tasks>

<verification>
<automated>cd /Users/rui/src/pg/ComfyUI/custom_nodes/comfyui_imgtools && pytest tests/ -x -v</automated>
</verification>

<success_criteria>
- FaceComposite registered in ComfyUI node mappings
- All 4 face nodes visible in ComfyUI under imgtools/face category
- Full pipeline produces natural-looking composited result
- All tests pass (full suite)
</success_criteria>

<output>
After completion, create `.planning/phases/04-compositing-and-integration/04-02-SUMMARY.md`
</output>
