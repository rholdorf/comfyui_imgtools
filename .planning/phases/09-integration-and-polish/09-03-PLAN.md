---
phase: 09-integration-and-polish
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - tests/test_integration_pipeline.py
autonomous: true
requirements:
  - INTG-01
  - INTG-02

must_haves:
  truths:
    - "Full pipeline (FaceModelBuilder -> FaceModelMorph -> FaceComposite) produces valid output tensors without exceptions"
    - "Pipeline output composited_image has same spatial dimensions as original input image"
    - "Pipeline output face_region_mask has expected shape (1, H, W)"
  artifacts:
    - path: "tests/test_integration_pipeline.py"
      provides: "E2E integration test for full pipeline chain"
      contains: "TestE2EPipeline"
  key_links:
    - from: "face_model_builder.py::build_model()"
      to: "face_model_morph.py::morph()"
      via: "face_model dict output -> input"
      pattern: "face_model"
    - from: "face_model_morph.py::morph()"
      to: "face_composite.py::composite()"
      via: "morphed_face, align_data outputs -> inputs"
      pattern: "morphed_face.*align_data"
---

<objective>
Create an end-to-end integration test that chains FaceModelBuilder -> FaceModelMorph -> FaceComposite, verifying the full pipeline produces valid output tensors without exceptions.

Purpose: Phase 9 Success Criterion 3 -- validate the full model-based pipeline works end-to-end.
Output: tests/test_integration_pipeline.py with E2E pipeline test passing.
</objective>

<execution_context>
@/Users/rui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-and-polish/09-RESEARCH.md

@face_model_builder.py
@face_model_morph.py
@face_composite.py
@tests/test_face_model_builder.py

<interfaces>
<!-- Node signatures the E2E test must chain -->

FaceModelBuilder.build_model(directory, yaw_threshold=45.0, pitch_threshold=30.0, save_path="")
  -> (face_model: dict, quality_report: str, preview: IMAGE tensor [1,512,512,3])

FaceModelMorph.morph(source_image, face_model, source_landmarks, source_align_data, strength=0.5, symmetrize=False)
  -> (morphed_face: IMAGE tensor [1,H,W,3], warp_mask: MASK tensor [1,H,W], align_data: dict)

FaceComposite.composite(original_image, morphed_face, align_data)
  -> (composited_image: IMAGE tensor [1,H,W,3], face_region_mask: MASK tensor [1,H,W])

<!-- Mock pattern from test_face_model_builder.py -->
@patch("utils.model_builder.process_image")
@patch("utils.model_builder.scan_images")

<!-- align_data dict structure (from FaceCropAlign) -->
align_data = {
    "crop_box": (x1, y1, x2, y2),
    "original_size": (orig_h, orig_w),
    "rotation_angle": float,
    "rotation_center": (cy, cx),
    "head_scale": float,
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create E2E integration pipeline test</name>
  <files>tests/test_integration_pipeline.py</files>
  <behavior>
    - test_full_pipeline_produces_valid_output: Chain FaceModelBuilder.build_model() -> FaceModelMorph.morph() -> FaceComposite.composite() with mocked MediaPipe. Assert: no exception raised, composited_image shape is (1, orig_H, orig_W, 3), face_region_mask shape is (1, orig_H, orig_W), all tensors are float32.
    - test_pipeline_with_error_model_graceful: Chain FaceModelBuilder.build_model() (returning error/empty model from 09-01 error handling) -> FaceModelMorph.morph() (should passthrough from 09-02 validation) -> FaceComposite.composite(). Assert: no exception, output equals passthrough of original image.
  </behavior>
  <action>
Create `tests/test_integration_pipeline.py` with class `TestE2EPipeline`.

**Test 1: test_full_pipeline_produces_valid_output**
1. Set up synthetic data:
   - Create a (1, 256, 256, 3) float32 torch tensor as `original_image` (random values)
   - Create a (1, 128, 128, 3) float32 tensor as `source_image` (simulating cropped face)
   - Create synthetic `source_landmarks` as numpy array shape (478, 3)
   - Create synthetic `source_align_data` dict with crop_box=(64, 64, 192, 192), original_size=(256, 256), rotation_angle=0.0, rotation_center=(128, 128), head_scale=1.0
2. Mock `utils.model_builder.scan_images` and `utils.model_builder.process_image` (same pattern as TestBuildFaceModel in test_face_model_builder.py):
   - mock_scan returns [tmp_path / "img.jpg"] (create the file with fake bytes)
   - mock_process returns a single ACCEPTED result with valid normalized_3d (478, 3) landmarks and head_dims dict
3. Instantiate FaceModelBuilder, call build_model(str(tmp_path))
4. Assert face_model is a non-empty dict with "canonical_landmarks" key
5. Instantiate FaceModelMorph, call morph(source_image, face_model, source_landmarks, source_align_data, strength=0.5)
6. Assert morphed_face shape is (1, 128, 128, 3) and dtype is float32
7. Instantiate FaceComposite, call composite(original_image, morphed_face, align_data)
8. Assert composited_image shape is (1, 256, 256, 3), face_region_mask shape is (1, 256, 256)

**Test 2: test_pipeline_with_error_model_graceful**
1. Mock build_face_model to raise ValueError("No accepted images")
2. Call FaceModelBuilder.build_model() -- should return ({}, "ERROR: ...", black_preview) per 09-01 error handling
3. Pass empty {} model to FaceModelMorph.morph() -- should return passthrough per 09-02 validation
4. Pass passthrough output to FaceComposite.composite()
5. Assert no exception, output image has same shape as source_image

**Important:** Import nodes from `comfyui_imgtools.face_model_builder`, `comfyui_imgtools.face_model_morph`, `comfyui_imgtools.face_composite`. For the morph call, also mock `face_model_morph.extract_landmarks` if the morph path requires landmark extraction (check the morph code path -- the source_landmarks input is passed directly, so no additional mock needed for the happy path).

For the morph step, the TPS warp needs valid landmark correspondence. Use the canonical_landmarks from the model as source_landmarks (shape (478, 2)) padded with a z=0 column to make (478, 3). This ensures the warp is an identity-like transform and won't crash.

Run: `conda run -n comfyui python -m pytest tests/test_integration_pipeline.py -x -v`
  </action>
  <verify>
    <automated>conda run -n comfyui python -m pytest tests/test_integration_pipeline.py -x -v && conda run -n comfyui python -m pytest tests/ -x -v</automated>
  </verify>
  <done>E2E integration test passes: full pipeline chain (FaceModelBuilder -> FaceModelMorph -> FaceComposite) produces valid output tensors. Error path also tested for graceful degradation. Full test suite passes with no regression.</done>
</task>

</tasks>

<verification>
conda run -n comfyui python -m pytest tests/test_integration_pipeline.py -x -v -k "e2e"
conda run -n comfyui python -m pytest tests/ -x -v
</verification>

<success_criteria>
- E2E pipeline test exists and passes
- Full pipeline chain produces valid tensors with correct shapes
- Error model path degrades gracefully (no crash)
- All existing tests continue to pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-and-polish/09-03-SUMMARY.md`
</output>
